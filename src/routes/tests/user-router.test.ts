import supertest from "supertest";
import userRouter from '../user-router'

describe('PUT /', () => {
    describe('Given a unique user', () => {
        // integrační testy -> volání api
        // before each zavolat login -> přibalit token
    })
})